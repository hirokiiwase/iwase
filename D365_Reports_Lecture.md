# Dynamics 365 帳票出力 完全講義ノート
## 〜 初心者からベテラン情シスまで 〜

作成日: 2026-05-28  
対象: D365 Customer Engagement (Sales / Service / Field Service) ユーザー・開発者

---

## 目次

1. [帳票とは何か（概念編）](#1-帳票とは何か概念編)
2. [D365 における帳票の全体像](#2-d365-における帳票の全体像)
3. [方法別 比較早見表](#3-方法別-比較早見表)
4. [初心者向け: Word テンプレート](#4-初心者向け-word-テンプレート)
5. [初中級者向け: Excel テンプレート](#5-初中級者向け-excel-テンプレート)
6. [中級者向け: SSRS レポート](#6-中級者向け-ssrs-レポート)
7. [中上級者向け: サードパーティ製品（SLERT 等）](#7-中上級者向け-サードパーティ製品slert-等)
8. [上級者向け: Plugin + カスタム生成](#8-上級者向け-plugin--カスタム生成)
9. [上級者向け: Azure Functions + QuestPDF（PDF 完全制御）](#9-上級者向け-azure-functions--questpdfpdf-完全制御)
10. [できること・できないこと 一覧](#10-できることできないこと-一覧)
11. [ベストプラクティス](#11-ベストプラクティス)
12. [Microsoft 標準推奨パターン](#12-microsoft-標準推奨パターン)
13. [よくある質問 Q&A](#13-よくある質問-qa)

---

## 1. 帳票とは何か（概念編）

### 「帳票」の定義

> 帳票 ＝ 業務上やり取りされる定型フォーマットの書類

```
帳票の例：
  ・見積書     ← 顧客に金額を提示する
  ・請求書     ← 代金の請求
  ・注文書     ← 仕入先への発注
  ・納品書     ← 商品の引き渡し証明
  ・契約書     ← 合意内容の記録
  ・報告書     ← 進捗・実績の報告
```

### なぜ D365 で帳票が必要か

D365 は「データを管理する」システムです。
しかし、ビジネスでは「データを紙・PDF・Excel で出力する」場面が必ず発生します。

```
D365 の役割
  ├── データの入力・管理（← D365 が得意）
  ├── 業務プロセスの自動化（← Power Automate が担う）
  └── データの出力・帳票化（← ここが今日のテーマ）
```

### 帳票出力の基本フロー

```
D365 のレコード（取引先・見積・注文 等）
          ↓
    データ取得（FetchXML / OData）
          ↓
    テンプレート or コードで整形
          ↓
    ファイル生成（Word / Excel / PDF）
          ↓
    ダウンロード or メール添付 or タイムラインに保存
```

---

## 2. D365 における帳票の全体像

### 帳票出力の5つのアプローチ

```
┌─────────────────────────────────────────────────────────────────┐
│                  D365 帳票出力 アプローチ全体図                  │
│                                                                 │
│  難易度: 低  ─────────────────────────────────→  難易度: 高    │
│                                                                 │
│  [Word         [Excel         [SSRS          [サード    [カスタム│
│   テンプレート]  テンプレート]   レポート]       パーティ]  実装]  │
│                                                                 │
│  ノーコード     ローコード      中程度コード    設定中心   フルコード│
│  標準機能       標準機能        標準機能       製品依存    自由度高 │
└─────────────────────────────────────────────────────────────────┘
```

### 選択の判断軸

| 判断軸 | 質問 |
|--------|------|
| 技術力 | 開発者がいるか？ |
| 自由度 | テンプレートで対応できるか？ |
| コスト | 追加費用をかけられるか？ |
| 運用 | 誰がテンプレートを管理するか？ |
| 量 | 1日何件の帳票が出るか？ |

---

## 3. 方法別 比較早見表

| 方法 | 難易度 | コスト | 自由度 | PDF対応 | 推奨場面 |
|------|--------|--------|--------|---------|---------|
| Word テンプレート | ★☆☆ | 無料 | 低 | △手動変換 | 契約書・提案書 |
| Excel テンプレート | ★☆☆ | 無料 | 中 | △手動変換 | 集計レポート |
| SSRS レポート | ★★★ | 無料 | 中 | ✅自動 | 大量印刷・一覧表 |
| SLERT 等サード | ★★☆ | 有料 | 高 | ✅自動 | Excel帳票が中心 |
| Plugin + カスタム | ★★★ | 開発費 | 最高 | ✅自動 | 複雑な業務帳票 |
| Azure Functions | ★★★★ | 開発費+Azure | 最高 | ✅自動 | 高品質PDF |

---

## 4. 初心者向け: Word テンプレート

### 概要

```
難易度: ★☆☆  コスト: 無料  標準機能: ✅
```

D365 に内蔵された機能。Word ファイルを「テンプレート」として登録し、
レコードのデータを差し込んで文書を生成します。

### 仕組み

```
Word (.docx) に差し込みフィールドを設定
        ↓
D365 にアップロード（設定 → テンプレート → ドキュメントテンプレート）
        ↓
レコード画面から「Word テンプレート」を実行
        ↓
フィールドが自動的に置換された Word ファイルをダウンロード
```

### できること

- ✅ 取引先名・住所・電話番号などの差し込み
- ✅ 繰り返し行（明細テーブル）
- ✅ 条件分岐（値が空の場合など）
- ✅ 画像の差し込み（ロゴ等）
- ✅ 関連レコードのデータ取得

### できないこと

- ❌ PDF で直接出力（Word ダウンロード後に手動で PDF 変換が必要）
- ❌ 複雑な計算式（SUM は可能、複雑な集計は困難）
- ❌ グラフの動的生成
- ❌ 複数レコードを1枚の帳票にまとめる処理

### 設定手順（初心者向け）

```
1. Word で帳票レイアウトを作成
2. 「差し込み文書」タブ → 「フィールドの挿入」
   ※ D365 のフィールド名（スキーマ名）を確認しておく
3. D365 にログイン → 設定 → テンプレート → ドキュメントテンプレート
4. 「新規」→ テンプレートの種類: Word → ファイルをアップロード
5. レコード画面の「...」→ Word テンプレート → テンプレートを選択
```

### ベストプラクティス

- テンプレートは**パワーユーザーが管理**できるように権限設計する
- テンプレート名は `[帳票名]_[バージョン]` など命名規則を統一する
- PDF 変換が必要な場合は Power Automate と組み合わせる

---

## 5. 初中級者向け: Excel テンプレート

### 概要

```
難易度: ★☆☆  コスト: 無料  標準機能: ✅
```

Word テンプレートの Excel 版。集計・グラフが得意。

### Word テンプレートとの違い

| | Word テンプレート | Excel テンプレート |
|--|-----------------|-----------------|
| 向いている帳票 | 文書形式（契約書等） | 表・集計形式 |
| 計算 | 弱い | 強い（Excel 関数が使える） |
| グラフ | 難しい | ✅ 動的グラフ |
| 印刷設定 | Word の印刷設定 | Excel の印刷設定 |

### できること

- ✅ Excel 関数（SUMIF、VLOOKUP 等）が使える
- ✅ ピボットテーブル
- ✅ グラフの動的生成
- ✅ 複数シートへのデータ展開

### できないこと

- ❌ PDF で直接出力
- ❌ セルの動的結合（レイアウトは固定）
- ❌ 1件のレコードで行数が変動するレイアウト（明細が0〜100行になる等は難しい）

---

## 6. 中級者向け: SSRS レポート

### 概要

```
難易度: ★★★  コスト: 無料（D365 に内蔵）  標準機能: ✅
```

SQL Server Reporting Services の略。Microsoft の帳票ツール。
D365 と深く統合されており、「レポート」として画面から実行できる。

### 特徴

```
SSRS の強み:
  ✅ PDF / Excel / Word / CSV など複数形式で出力
  ✅ 大量データの一括出力に強い
  ✅ パラメータ（期間指定等）で絞り込み
  ✅ 定期的な自動実行・メール送信
  ✅ D365 標準の請求書・注文書は SSRS で作られている
```

### 仕組み

```
Visual Studio（RDL ファイルを作成）
        ↓
FetchXML でデータソースを定義
        ↓
レイアウト（テーブル・グラフ）を設計
        ↓
D365 にインポート（設定 → レポート）
        ↓
レコード一覧 or レポート画面から実行
```

### RDL ファイルとは

```
RDL = Report Definition Language
= Microsoft の帳票レイアウト定義ファイル（XML 形式）

Visual Studio の「Reporting Services Project」テンプレートで作成
```

### できること

- ✅ 複数エンティティを JOIN したデータ
- ✅ 複数ページにわたる帳票（ページ番号も自動）
- ✅ PDF での直接ダウンロード
- ✅ 印刷プレビュー
- ✅ グループ化・集計・小計・合計
- ✅ 条件付き書式（赤字など）

### できないこと

- ❌ 動的なセル結合（固定レイアウトが前提）
- ❌ 複雑な Excel 形式（SSRS は印刷向き）
- ❌ Power Apps ポータルからの実行（モデル駆動型アプリ専用）

### 推奨場面

- 月次・年次の集計レポート
- 管理職向けのダッシュボード印刷
- 大量の帳票を一括で出力する場合
- D365 標準の帳票をカスタマイズする場合

---

## 7. 中上級者向け: サードパーティ製品（SLERT 等）

### 概要

```
難易度: ★★☆  コスト: 有料（ライセンス費用）  外部製品: アドオン
```

Microsoft 以外のベンダーが提供する帳票ツール。
特に Excel ベースの帳票が得意。

### SLERT（スラート）とは

- 日本のベンダー（SourceLink）が提供
- D365 CE 向けの Excel 帳票アドオン
- Excel テンプレートを作成・アップロードするだけで帳票出力
- マクロ・PDF 変換・自動メール送信対応
- 30日無料トライアルあり

### SLERT が向いている場面

```
✅ すでに Excel で帳票フォーマットが決まっている
✅ 経理・総務がテンプレートを自分で管理したい
✅ D365 標準の Excel テンプレートでは対応できないレイアウト
✅ PDF での自動出力が必要
✅ 開発者を使わずに済ませたい
```

### サードパーティ選定のポイント

| チェック項目 | 内容 |
|------------|------|
| D365 バージョン対応 | 使用中の D365 バージョンに対応しているか |
| サポート体制 | 日本語サポートがあるか |
| ライセンス形態 | ユーザー数課金か、環境課金か |
| 内製化できるか | 製品依存になりすぎないか |
| 将来性 | ベンダーの継続性 |

---

## 8. 上級者向け: Plugin + カスタム生成

### 概要

```
難易度: ★★★  コスト: 開発費  自由度: 最高
```

C# で Plugin を開発し、レコード作成・更新のタイミングで
自動的に帳票（Excel / CSV / PDF）を生成して Annotation に保存する方法。

### Plugin の基本概念

```
Plugin = D365 のイベントに反応して動く C# プログラム

例:「Account レコードが作成されたら自動で見積書 Excel を生成する」

トリガーの種類:
  ・Create（作成時）
  ・Update（更新時）
  ・Delete（削除時）
  ・カスタムアクション（任意のタイミング）

実行タイミング（Stage）:
  ・PreValidation (10) : DB 書き込み前
  ・PreOperation  (20) : DB 書き込み直前
  ・PostOperation (40) : DB 書き込み後 ← 帳票生成はここ

実行モード:
  ・Synchronous  : 同期（ユーザーを待たせる）← 軽い処理のみ
  ・Asynchronous : 非同期（バックグラウンド）← 帳票生成はこちら
```

### Plugin サンドボックスの制約（重要）

```
Plugin は「サンドボックス」という制限環境で動く

制限の内容:
  ❌ 大きな外部 DLL を使えない（ILRepack でスタックオーバーフローが起きやすい）
  ❌ ファイルシステムへの書き込み不可
  ❌ 外部ネットワーク接続は制限あり（設定で緩和可能）
  ❌ メモリ・CPU に上限あり

使える技術:
  ✅ .NET Framework 4.6.2 標準ライブラリ
  ✅ System.IO.Compression（ZIP 操作 = xlsx 生成に使える）
  ✅ Dataverse SDK（他レコードの取得・作成）
  ✅ HttpClient（外部 API 呼び出し ← サンドボックス設定で許可が必要）
```

### Excel (xlsx) の手書き生成

```
xlsx ファイルの正体 = ZIP ファイルの中に XML が入っているだけ

構造:
  mitsumori.xlsx (ZIP)
  ├── [Content_Types].xml    ← ファイル種別の定義
  ├── _rels/.rels            ← ファイル間の関係定義
  ├── xl/
  │   ├── workbook.xml       ← ブック全体の構造
  │   ├── worksheets/
  │   │   └── sheet1.xml     ← セルのデータ（ここにデータを書く）
  │   ├── styles.xml         ← 書式（色・フォント・罫線）
  │   └── sharedStrings.xml  ← 文字列の共有リスト
  └── _rels/workbook.xml.rels ← シート間の関係

→ System.IO.Compression で ZIP を作成し、各 XML を書けば xlsx が完成
→ 外部ライブラリ不要 → ILRepack 不要
```

### Annotation（添付ファイル）への保存

```csharp
var note = new Entity("annotation")
{
    ["objectid"]       = new EntityReference("account", accountId),
    ["objecttypecode"] = "account",
    ["subject"]        = "御見積書_20260528",
    ["filename"]       = "mitsumori.xlsx",
    ["mimetype"]       = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
    ["documentbody"]   = Convert.ToBase64String(xlsxBytes), // ← Base64 で保存
};
svc.Create(note);
```

これにより、レコードのタイムライン（メモ欄）にファイルが添付されます。

### Plugin 登録の流れ

```
1. C# でコーディング（IPlugin インターフェースを実装）
2. dotnet build でビルド
3. sn.exe で強名キー（.snk）を生成
4. csproj に署名設定を追加して再ビルド
5. Plugin Registration Tool を起動
6. D365 環境に接続
7. 「Register New Assembly」でDLL をアップロード
8. 「Register New Step」でトリガー条件を設定
```

---

## 9. 上級者向け: Azure Functions + QuestPDF（PDF 完全制御）

### なぜ Azure Functions が必要か

```
Plugin サンドボックスの制約により:
  → PDF ライブラリ（QuestPDF 等）を Plugin 内で使うと
    ILRepack のスタックオーバーフローが発生

解決策:
  → PDF 生成処理を Plugin の外（Azure Functions）に出す
  → Plugin は Azure Functions に HTTP でデータを送るだけ
  → Azure Functions は制約なしで QuestPDF を使って PDF 生成
```

### アーキテクチャ

```
┌─────────────────────────────────────────────────────────┐
│  Dynamics 365                                           │
│                                                         │
│  Account レコード作成                                    │
│      ↓                                                  │
│  Plugin（Post-Create, Async）                           │
│      ↓ HTTP POST（JSON: 顧客名・明細等）                │
└─────────────────────────────────────────────────────────┘
              ↓ インターネット経由
┌─────────────────────────────────────────────────────────┐
│  Azure Functions                                        │
│                                                         │
│  GeneratePdf 関数（HTTP トリガー）                       │
│      ↓                                                  │
│  QuestPDF で A4 見積書 PDF を生成                        │
│      ↓                                                  │
│  PDF バイト配列を返す                                    │
└─────────────────────────────────────────────────────────┘
              ↓ PDF バイト受信
┌─────────────────────────────────────────────────────────┐
│  Dynamics 365                                           │
│                                                         │
│  Plugin がバイトを Annotation に保存                     │
│  → タイムラインに PDF が添付される                       │
└─────────────────────────────────────────────────────────┘
```

### QuestPDF とは

```
QuestPDF = C# で PDF を「コードでレイアウト」できるライブラリ（無料・OSS）

特徴:
  ✅ コードで HTML/CSS に近い感覚でレイアウト
  ✅ 日本語フォントの埋め込み対応
  ✅ A4 センタリング・余白設定が簡単
  ✅ ページをはみ出したら自動改ページ
  ✅ 商用利用も Community ライセンスで無料

コード例:
  Document.Create(container => {
      container.Page(page => {
          page.Size(PageSizes.A4);
          page.Margin(40);
          page.Content().Column(col => {
              col.Item().AlignCenter().Text("御見積書").FontSize(22).Bold();
              col.Item().Table(table => { ... });
          });
      });
  }).GeneratePdf();
```

### Azure Functions のコスト

```
従量課金プランの無料枠（月あたり）:
  ・100万回 の実行    → 無料
  ・40万 GB-秒       → 無料

業務用途（月 1,000〜10,000 回）→ 実質無料〜数百円
```

### セキュリティ考慮事項

```
本番環境での推奨設定:
  1. authlevel を anonymous → function に変更（Function Key 認証）
  2. Plugin の HttpClient に Function Key をヘッダーで付与
  3. Azure Functions の IP 制限（D365 の IP を許可リストに追加）
  4. HTTPS 必須（既定で HTTPS のみ）
```

---

## 10. できること・できないこと 一覧

### 全方法共通でできること

- ✅ D365 のデータを帳票に差し込む
- ✅ レコードのタイムラインにファイルを添付
- ✅ ボタンクリックで手動実行
- ✅ レコード作成・更新時に自動実行（Plugin/Power Automate）

### 方法ごとのできないこと

| 制限 | Word | Excel | SSRS | サード | Plugin | Azure Func |
|------|------|-------|------|--------|--------|-----------|
| PDF自動出力 | ❌ | ❌ | ✅ | ✅ | △ | ✅ |
| 動的セル結合 | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ |
| 外部データ取得 | ❌ | ❌ | △ | △ | ✅ | ✅ |
| 完全なレイアウト制御 | ❌ | △ | △ | △ | ✅ | ✅ |
| コードなしで管理 | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |

### D365 標準機能の限界

```
「標準機能だけでは対応できない」よくある要求:

  1. 見積書を PDF で自動送信したい
     → Power Automate + Word テンプレート + PDF 変換コネクタ が必要

  2. セルが動的に結合される Excel 帳票を出したい
     → SLERT または Plugin カスタム開発が必要

  3. 複数レコードを1枚の帳票にまとめたい（例:1日分の注文を1枚に）
     → SSRS または Plugin カスタム開発が必要

  4. 帳票に QR コードを含めたい
     → Plugin または Azure Functions で生成が必要

  5. 電子署名・印影を自動で入れたい
     → カスタム開発 + 画像合成が必要
```

---

## 11. ベストプラクティス

### 方法の選び方（フローチャート）

```
帳票の要件は？
    │
    ├─ 既存の Word/Excel フォーマットがある
    │       │
    │       ├─ PDF 自動化が不要 → Word/Excel テンプレート（標準機能）
    │       └─ PDF 自動化が必要 → Power Automate + Word テンプレート
    │
    ├─ Excel で複雑なレイアウト（セル結合・マクロ）が必要
    │       → SLERT 等サードパーティ
    │
    ├─ 一覧・集計系の帳票（月次レポート等）
    │       → SSRS レポート
    │
    └─ 完全にカスタムな帳票（業務特化・高品質 PDF）
            ├─ Excel 形式 → Plugin + 手書き xlsx
            └─ PDF 形式  → Azure Functions + QuestPDF
```

### 開発・運用のベストプラクティス

#### テンプレート管理
- テンプレートのバージョン管理をする（ファイル名に日付を入れる）
- テンプレート変更者を限定する（権限設定）
- テスト環境でテンプレートを検証してから本番に反映する

#### Plugin 開発
- 帳票生成は必ず **Asynchronous（非同期）** で実装する
  （重い処理を同期で動かすとタイムアウト・ユーザー体験悪化）
- `tracing.Trace()` でログを残す（デバッグに必須）
- エラーハンドリングをしっかり書く
- Plugin Trace Viewer でログを定期確認する

#### Azure Functions
- 本番環境では **Function Key 認証** を使う（anonymous は NG）
- Application Insights でログ・エラーを監視する
- タイムアウト設定に注意（従量課金プランは最大 10分）

#### 全般
- 帳票のファイル名に日付・レコード番号を含める
  例: `mitsumori_A0001_20260528.pdf`
- 大量帳票の一括生成は業務時間外に実行する
- 帳票の文字エンコードは **UTF-8** を使う（CSV は BOM 付きで Excel に優しい）

---

## 12. Microsoft 標準推奨パターン

### Microsoft が推奨する帳票出力の考え方

```
Microsoft の立場:
  「できる限り標準機能を使う」「Power Platform でローコードに」

標準の推奨ルート:
  1. シンプルな帳票 → Word/Excel テンプレート
  2. 定型レポート  → SSRS
  3. 自動化        → Power Automate と組み合わせる
  4. 複雑な処理    → Azure Logic Apps または Azure Functions
```

### Power Automate との組み合わせ（標準推奨）

```
Power Automate でできる帳票自動化:
  ✅ レコード作成時に Word テンプレートで文書生成
  ✅ 生成した Word を PDF に変換（OneDrive コネクタ）
  ✅ PDF をメールで自動送信
  ✅ SharePoint に自動保存

コード不要でここまでできる → 小規模なら最適解
```

### D365 帳票の標準機能ロードマップ的な考え方

```
フェーズ1（導入初期）: Word/Excel テンプレートで対応
フェーズ2（業務定着後）: SSRS で集計レポートを整備
フェーズ3（自動化推進）: Power Automate で帳票送信を自動化
フェーズ4（高度化）: 複雑な要件はカスタム開発で対応
```

---

## 13. よくある質問 Q&A

### Q1. 帳票を PDF で出したいのですが、D365 の標準機能でできますか？

**A:** SSRS レポートは PDF 出力に対応しています。Word/Excel テンプレートは直接 PDF 出力できませんが、Power Automate の「ファイルの変換」アクションで PDF に変換できます。完全自動化したい場合は Power Automate と組み合わせる方法が Microsoft 標準推奨です。

---

### Q2. Word テンプレートと Excel テンプレートはどちらが良いですか？

**A:** 帳票の目的で選んでください。
- **文書形式**（見積書・契約書・通知書）→ Word テンプレート
- **表・集計形式**（レポート・一覧）→ Excel テンプレート

---

### Q3. Plugin 開発はどのくらいの技術力が必要ですか？

**A:** C# の基礎知識（クラス・メソッド・例外処理）があれば習得できます。D365 固有の知識（IPlugin インターフェース・IOrganizationService・FetchXML）は学習が必要ですが、半日〜1日の学習で基本的な Plugin は書けるようになります。

---

### Q4. SLERT と標準機能はどちらが良いですか？

**A:** 状況によります。
- **Excel ベースの帳票が多い・現場が Excel に慣れている** → SLERT が向いています
- **予算が限られている・帳票の種類が少ない** → 標準機能で対応を検討
- **将来的な拡張性が重要** → カスタム開発も視野に

---

### Q5. 帳票生成に失敗した場合、どうやって調査しますか？

**A:** 以下の順で確認します。
1. **Plugin Trace Viewer**（XrmToolBox）でエラーログを確認
2. **Application Insights**（Azure Functions の場合）でエラー詳細を確認
3. テスト環境で再現させ、`tracing.Trace()` ログを追う

---

### Q6. 大量帳票（1,000件/日）を自動生成する場合の注意点は？

**A:**
- Plugin の **Asynchronous** 実行を使い、業務時間への影響を最小化する
- Azure Functions の場合、**並列実行数の上限**（従量課金: 200並列）に注意
- ファイルサイズが大きい場合、Annotation の `documentbody` には上限（32MB）があるので注意

---

### Q7. 電子帳票保存法（電子帳簿保存法）への対応は？

**A:** D365 で生成した帳票を電子帳簿保存法に対応させるには、
- **タイムスタンプ**の付与
- **訂正・削除の防止**
- **検索要件**への対応

が必要です。Annotation への保存だけでは要件を満たさない場合があるため、法務・税理士と連携した上でシステム設計を行ってください。

---

## 付録: 用語集

| 用語 | 説明 |
|------|------|
| IPlugin | D365 Plugin が実装するインターフェース |
| Annotation | D365 の添付ファイル・メモを管理するエンティティ |
| documentbody | Annotation の添付ファイル本体（Base64 文字列） |
| ILRepack | 複数の DLL を1つにまとめるツール |
| SNK ファイル | 強名（Strong Name）署名キーファイル |
| FetchXML | D365 のデータ取得クエリ言語（SQL の D365 版） |
| サンドボックス | Plugin が動く制限付き実行環境 |
| SSRS | SQL Server Reporting Services（Microsoft の帳票ツール） |
| RDL | SSRS のレポート定義ファイル形式 |
| QuestPDF | C# 向け PDF 生成ライブラリ（OSS・無料） |
| Azure Functions | Azure 上のサーバーレス実行環境 |
| 従量課金プラン | 実行回数に応じた課金（月100万回まで無料） |
